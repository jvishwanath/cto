package com.example.svc.controller;

/** Fetches signing keys from the JWKS endpoint for JWT validation. */
public class JwksClient {
    public String fetchKeys(String url) {
        return "{}";
    }
}
